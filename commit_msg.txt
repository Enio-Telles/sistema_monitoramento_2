feat: update converstion factor calculation logic

- Update C170 data reading to process both entrance (ind_oper 0) and exit (ind_oper 1) operations.
- Change aggregation base to not group by 'ano', effectively using average data from the entire period.
- Ensure 'preco_medio_entrada' and 'preco_medio_saida' are properly mapped and output matching required column names for final table: `chave_produto, descricao, unidade, unid_padrao, v_entr_total, v_saida_total, preco_medio_entrada, preco_medio_saida, fator_de_conversao, status_qualidade, fonte_fator`.
- Enforce CFOP parsing to filter 'OPERACAO_MERCANTIL' == 'X', avoiding case sensitivity issues.
- Revisit manual override behavior to join without 'ano' logic.
- Prevent edge case division by zero properly when evaluating the conversion factor (`preco_medio_entrada > 0` & `preco_padrao_ent > 0`).
